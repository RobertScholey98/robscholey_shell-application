import { MonoMark } from '@robscholey/shell-kit/ui';

/**
 * Template app-card identity visual — a corner-bracketed `N` tile, the
 * minimal mark the template uses for its own brand.
 */
export function TemplateVisual() {
  return <MonoMark letter="N" size="md" />;
}
